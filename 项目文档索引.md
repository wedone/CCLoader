# 项目文档索引

## 索引用途

本索引是 CCLoader 烧录器项目的**文档导航中枢**和**元数据单一数据源**。新 AI 接手项目时，**只需读本文件**即可了解项目全貌，知道该读哪个文档获取详细信息。

## 如何使用本索引

1. **新 AI 接手**：先读本索引 → 再读 [Documents/项目进展.md](Documents/项目进展.md) 了解当前状态 → 按需查阅其他文档
2. **遇到问题**：根据问题类型查阅对应文档（见下表"何时阅读"列）
3. **维护索引**：新增文档时在本索引中添加条目，保持索引与实际文件一致

## 项目基本信息（单一数据源）

> **以下元数据只在本节维护，其他文档引用本节，不重复定义。**

| 项 | 值 |
|----|-----|
| 项目 | CCLoader WebUI - CC2530 烧录+监控+抓包一体机 |
| 主控 | NodeMCU ESP8266 (ESP-12E, 4MB Flash, 80KB RAM) |
| 目标芯片 | CC2530F256 (256KB Flash, Zigbee SoC) |
| 当前版本 | v1.5 |
| 当前分支 | master |
| GitHub 仓库 | https://github.com/wedone/CCLoader.git |
| 编译工具 | PlatformIO (ESP8266 Arduino Core 3.1.2) |
| 协议基础 | CCLib (CC Debug Interface, GPIO bit-bang) |
| 后端 | ESP8266WebServer (端口 80) + WiFiServer SSE (端口 81) |
| 前端 | 原生 HTML/CSS/JS + marked.js CDN（markdown 渲染） |
| 文件系统 | LittleFS (1MB 区域) |
| 部署方式 | OTA 远程升级（首次 USB 烧录后免拔线） |

## 文档清单

| 文档 | 何时阅读 | 内容摘要 |
|------|----------|----------|
| [Documents/项目进展.md](Documents/项目进展.md) | **必读** - 接手项目第一步 | 当前状态、已完成功能、待办事项、已知问题 |
| [Documents/硬件设计.md](Documents/硬件设计.md) | 需要查看引脚接线/外设信息 | 元件清单、引脚对照表、接线示意图、GPIO3 复用、LED 状态、CC Debug 协议原理 |
| [Documents/API参考.md](Documents/API参考.md) | 需要查看 HTTP API / SSE 事件 | 三态状态机、REST 端点详解、SSE 事件类型、互斥规则、Agent 自动化示例 |
| [Documents/编译烧录说明.md](Documents/编译烧录说明.md) | 需要编译/烧录/OTA 升级时 | PlatformIO 编译、USB 首次烧录、OTA 远程升级、web_assets.h 生成流程、版本号位置 |
| [Documents/BUG修复记录.md](Documents/BUG修复记录.md) | 遇到BUG或需要了解历史问题 | 按版本记录的BUG现象、根因、修复方案、相关代码位置 |
| [Documents/版本更新规则.md](Documents/版本更新规则.md) | 需要发版/修改版本号时 | 版本号规范、发版判定标准、版本号位置（双处同步）、发版更新清单 |
| [CHANGELOG.md](CHANGELOG.md) | 需要了解版本变更历史 | 版本变更记录，按版本号组织（Keep a Changelog 格式） |
| [README.md](README.md) | 需要查看用户操作手册 | 9 章操作教程：功能/接线/部署/浏览器/API/故障排查/FAQ |
| [data/help.md](data/help.md) | 需要查看嵌入式帮助内容 | 设备内显示的帮助页（markdown 渲染），API 用法 + FAQ |
| [CCLoader_Sniffer抓包改造需求.md](CCLoader_Sniffer抓包改造需求.md) | 需要了解 sniffer 抓包功能设计 | ZBOSS sniffer 协议、环形缓冲、pcap 生成、Serial 双工冲突陷阱 |

## 快速定位

| 我需要... | 去哪里找 |
|-----------|----------|
| 了解项目当前进度 | [Documents/项目进展.md](Documents/项目进展.md) |
| 查看当前版本号 | 本文件「项目基本信息」章节 |
| 查看某个BUG怎么修的 | [Documents/BUG修复记录.md](Documents/BUG修复记录.md) |
| 编译固件 | [Documents/编译烧录说明.md](Documents/编译烧录说明.md) |
| OTA 升级 | [Documents/编译烧录说明.md](Documents/编译烧录说明.md) OTA 章节 |
| 查看硬件引脚接线 | [Documents/硬件设计.md](Documents/硬件设计.md) |
| 查看 HTTP API 端点 | [Documents/API参考.md](Documents/API参考.md) |
| 查看 SSE 事件类型 | [Documents/API参考.md](Documents/API参考.md) SSE 章节 |
| 查看 WebUI 操作流程 | [README.md](README.md) |
| 查看版本变更历史 | [CHANGELOG.md](CHANGELOG.md) |
| 发版/修改固件版本号 | [Documents/版本更新规则.md](Documents/版本更新规则.md) + 本文件「发版更新清单」章节 |
| 修改 WebUI 界面 | [Documents/编译烧录说明.md](Documents/编译烧录说明.md) WebUI 资源更新章节 |
| 查看 sniffer 抓包设计 | [CCLoader_Sniffer抓包改造需求.md](CCLoader_Sniffer抓包改造需求.md) |
| 查看源码主文件 | `src/CCLoader.ino` |
| 查看前端代码 | `data/app.js` / `data/index.html` / `data/style.css` |

## 发版更新清单（单一数据源）

**修复BUG或增减功能后编译烧录，即为一次发版，必须递增版本号并同步更新以下文件：**

| 序号 | 文件 | 更新内容 |
|------|------|----------|
| 1 | `data/index.html` | "固件版本: vX.Y" 改为新版本号（前端显示用） |
| 2 | `src/CCLoader.ino` | `FIRMWARE_VERSION` 宏改为新版本号（API 返回用） |
| 3 | 运行 `python tools/gen_web_assets.py` | 重新生成 `src/web_assets.h`（从 data/ 同步） |
| 4 | `CHANGELOG.md` | 从 [Unreleased] 移到 [X.Y] 并添加日期 |
| 5 | `Documents/项目进展.md` | 更新"当前状态"中的版本号 |
| 6 | `项目文档索引.md` | 更新本文件「项目基本信息」中的"当前版本" |

### 版本号规则

采用语义化版本 `vX.Y`（修订号为 0 时省略，首次仅修复 BUG 时启用三段式 `vX.Y.Z`）：

| 版本段 | 何时递增 | 示例 |
|--------|----------|------|
| 主版本 (X) | 重大架构变更、不兼容修改 | v0→v1 |
| 次版本 (Y) | 新增功能、向后兼容 | v1.2→v1.3 |
| 修订号 (Z) | BUG修复、小调整 | v1.0.0→v1.0.1（暂未启用） |

### 编译标识（自动生成）

`/api/status` 返回 `build_time` 字段，由编译器 `__DATE__ " " __TIME__` 宏自动生成（如 `"Jul 30 2026 16:36:35"`），无需手动维护。用于区分同版本号的不同编译产物。

## 硬件约束（强制）

> **烧录 ESP8266 固件前必须遵守，否则烧录失败或设备异常。**

| 约束 | 说明 |
|------|------|
| **GPIO3 拔线** | USB 烧录 ESP8266 前必须拔掉 GPIO3 与 CC2530 TX 的连接线，否则 CC2530 启动日志干扰 ESP8266 bootloader 握手 |
| **CC2530 复位** | 进入 sniffer/monitor 模式前必须复位 CC2530，确保目标固件从干净状态启动 |
| **RAM 上限** | ESP8266 仅 80KB RAM，静态缓冲不超过 16KB（32KB 会导致 WiFi 无法启动 + boot loop） |
| **sniffer 模式禁用 Serial.write** | SNIFFING 状态下绝对不能调用 Serial.write/printf/println，会导致 RX 失效（Serial.available() 始终返回 0） |

## 文档维护规范

1. **新增文档**：放入 `Documents/` 目录，在本索引添加条目
2. **更新文档**：修改后同步更新本索引的"内容摘要"列
3. **元数据单一数据源**：版本号、硬件等元数据**只在本文件「项目基本信息」章节维护**，其他文档引用不重复
4. **文档命名**：中文命名，见名知意
5. **commit引用**：文档中引用commit时用短hash，如 `7c767b9`

> **AI 操作准则**：修复BUG或增减功能后编译烧录前，必须先按「发版更新清单」更新版本号及相关文档，不得跳过。
